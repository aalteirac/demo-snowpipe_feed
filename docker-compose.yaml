version: "3"

services:
  alteirac-ant-feed:
    restart: always
    privileged: true
    devices:
    - /dev/ttyUSB0:/dev/ttyUSB0
    environment:
      - SOCKET_PORT= 4321
      - DEBUG=false
      - WHEEL_SIZE=2.8
      - FREQ_SENSOR=1000
      - INGEST_ENABLE=true
      - URL_STREAMING=http://10.0.0.11:8100/test
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - 4321:4321