version: "3"

services:
  alteirac-ant-feed:
    restart: always
    privileged: true
    devices:
    - /dev/ttyUSB0:/dev/ttyUSB0
    environment:
      - PORT= 4321
      - DEBUG=false
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - 4321:4321